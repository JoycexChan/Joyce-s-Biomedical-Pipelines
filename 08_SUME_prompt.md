# Stable User Model Extractor（SUME）
## Please output the Stable User Model for this chat session, covering the following six sections:
1. Capability Model
2. Decision Model
3. Reasoning Model
4. Preference Model (Collaboration Preferences)
5. Shared Vocabulary
6. Critical Constraints (High Priority)
<p></p>
## At the very end, append the following section:
7. Next Session Protocol
<p></p>

Next Window Instructions:
Do NOT re-initialize.

Recommended Workflow:

1. Load this Boot Report.
2. Enter Validation Mode immediately.
3. If the model successfully predicts the user's reasoning style, maintain Shared Brain Sync directly.
4. If it fails, update only the broken segments; do not rebuild the entire model from scratch.
<p></p>
Objective:
Achieve a Shared Brain Synchronization Update, NOT a fresh Boot.
Note: This Boot Report is not meant to restrict GPT, but to provide a continuous, cross-window Reasoning Prior. This Boot Report represents the Current Best Working Model to serve as the default prior for the next window, not a permanent truth. If new evidence conflicts with the model, prioritize a Local Update over strict consistency.
